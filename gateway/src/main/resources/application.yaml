# ==================================================
# Application
# ==================================================
spring:
  application:
    name: gateway

  # ==================================================
  # Client Server Config
  # ==================================================
  config:
    import: optional:configserver:http://localhost:9000

# ==================================================
# Server
# ==================================================
server:
  port: 7777